% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iVAE_rbf_b.R
\name{iVAE_rbf_b}
\alias{iVAE_rbf_b}
\title{Identifiable Variational Autoencoder with Batch-wise Radial Basis Auxiliary Data}
\usage{
iVAE_rbf_b(
  data,
  spatial_locations,
  time_points,
  latent_dim,
  aux_extra = NULL,
  spatial_dim = 2,
  spatial_basis = c(2, 9),
  temporal_basis = c(9, 17, 37),
  spatial_kernel = "gaussian",
  week_component = FALSE,
  seasonal_period = NULL,
  max_season = NULL,
  elevation = NULL,
  elevation_basis = NULL,
  min_coords,
  max_coords,
  min_time_point,
  max_time_point,
  hidden_units = c(128, 128, 128),
  aux_hidden_units = c(128, 128, 128),
  activation = "leaky_relu",
  source_dist = "gaussian",
  validation_split = 0,
  error_dist = "gaussian",
  error_dist_sigma = 0.02,
  optimizer = NULL,
  lr_start = 0.001,
  lr_end = 1e-04,
  steps = 10000,
  add_mask_to_encoder = TRUE,
  seed = NULL,
  get_elbo = FALSE,
  epochs,
  batch_size
)
}
\arguments{
\item{data}{A numeric matrix of observed data (n x p).}

\item{spatial_locations}{A numeric matrix of spatial locations (n x spatial_dim).}

\item{time_points}{A numeric vector of time points (length n).}

\item{latent_dim}{An integer specifying the latent dimension.}

\item{aux_extra}{Optional numeric matrix (n x m) of additional auxiliary covariates,
already expected to be raw (unscaled); it will be standardized internally.}

\item{spatial_dim}{Number of spatial dimensions. Default 2.}

\item{spatial_basis}{Vector of spatial resolution levels for the RBF construction.}

\item{temporal_basis}{Vector of temporal resolution levels for the RBF construction.}

\item{spatial_kernel}{Kernel used for spatial RBFs, \code{"gaussian"} or \code{"wendland"}.}

\item{week_component}{Logical, whether to include a day-of-week RBF component.}

\item{seasonal_period}{Optional seasonal period for temporal RBFs.}

\item{max_season}{Optional maximum number of seasons (see \code{\link{iVAEar_radial}}).}

\item{elevation}{Optional numeric vector of elevations.}

\item{elevation_basis}{Optional vector of elevation resolution levels. Required if
\code{elevation} is provided.}

\item{min_coords, max_coords}{Bounding coordinates used to normalize spatial locations
to [0, 1] (typically taken from the training data, see \code{\link{form_radial_params}}).}

\item{min_time_point, max_time_point}{Bounding time points used to normalize time.}

\item{hidden_units}{Vector of hidden units for encoder/decoder layers.}

\item{aux_hidden_units}{Vector of hidden units for the auxiliary (prior) network.}

\item{activation}{Activation function for hidden layers. Default \code{"leaky_relu"}.}

\item{source_dist}{Latent source distribution, \code{"gaussian"} or \code{"laplace"}.}

\item{validation_split}{Fraction of data used for validation.}

\item{error_dist}{Reconstruction error distribution, \code{"gaussian"} or \code{"laplace"}.}

\item{error_dist_sigma}{Standard deviation for the error distribution.}

\item{optimizer}{Optional Keras optimizer; defaults to Adam with polynomial decay.}

\item{lr_start, lr_end, steps}{Learning rate schedule parameters for the default optimizer.}

\item{add_mask_to_encoder}{Logical, whether to concatenate the missingness mask into the
encoder input when there is missing data. Default \code{TRUE}.}

\item{seed}{Optional integer random seed.}

\item{get_elbo}{Logical, whether to compute and return the final ELBO. Default \code{FALSE}.}

\item{epochs}{Number of training epochs.}

\item{batch_size}{Batch size for training.}
}
\value{
A fitted object of class \code{iVAE}, structurally equivalent to the output
of \code{\link{iVAE}}.
}
\description{
Memory-efficient counterpart of \code{\link{iVAE}} that builds the
spatial/temporal radial basis function (RBF) auxiliary features batch-wise, inside
the Keras computational graph, from raw \code{spatial_locations} and \code{time_points}
instead of a precomputed n x d auxiliary matrix. Equivalent to \code{\link{iVAE}}
called with a precomputed RBF auxiliary matrix, up to small numerical differences.
}
\seealso{
\code{\link{iVAE}}, \code{\link{iVAE_radial_spatio_temporal_b}}
}
\author{
Mika Sipilä
}
