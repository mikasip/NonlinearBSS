% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GiVAE.R
\name{GiVAE}
\alias{GiVAE}
\title{Graph-structured Identifiable Variational Autoencoder}
\usage{
GiVAE(
  data,
  aux_data,
  latent_dim,
  adj_list,
  learn_edge_weights = FALSE,
  rho_max = 0.9,
  hidden_units = c(128, 128, 128),
  aux_hidden_units = c(128, 128, 128),
  edge_hidden_units = c(64, 64),
  activation = "leaky_relu",
  source_dist = "gaussian",
  error_dist = "gaussian",
  error_dist_sigma = 0.02,
  optimizer = NULL,
  lr_start = 0.001,
  lr_end = 1e-04,
  steps = 10000,
  seed = NULL,
  epochs,
  batch_size,
  validation_split = 0
)
}
\arguments{
\item{data}{N x P matrix of observed data.}

\item{aux_data}{N x K matrix of auxiliary data (e.g. Laplacian eigenvectors).}

\item{latent_dim}{Integer number of latent components.}

\item{adj_list}{Either a single adjacency list (list of length N, each
element a 1-based integer vector of neighbor indices for that observation)
or a list of such adjacency lists, one per graph.}

\item{learn_edge_weights}{Logical. If TRUE, learn an MLP-based attention model
for asymmetric edge weights using \eqn{(u_i, u_j)}. Default FALSE.}

\item{rho_max}{Numeric in (0,1). Hard upper bound on the spatial coupling
parameter \eqn{\rho}. Default 0.9.}

\item{hidden_units}{Integer vector; hidden layer sizes for encoder/decoder.}

\item{aux_hidden_units}{Integer vector; hidden layer sizes for prior-variance
and rho networks.}

\item{edge_hidden_units}{Integer vector; hidden layer sizes for edge weight
model (used only when \code{learn_edge_weights = TRUE}).}

\item{activation}{Activation function string. Default "leaky_relu".}

\item{source_dist}{Latent distribution: "gaussian" or "laplace".}

\item{error_dist}{Reconstruction distribution: "gaussian", "laplace", or
"poisson". For "poisson", the decoder outputs log-rates and data is not
standardised.}

\item{error_dist_sigma}{Scale parameter for gaussian/laplace error (ignored
for poisson). Default 0.02.}

\item{optimizer}{Optional Keras optimizer; default is Adam with polynomial
learning-rate decay.}

\item{lr_start, lr_end, steps}{Learning-rate schedule parameters.}

\item{seed}{Optional integer seed.}

\item{epochs, batch_size}{Training parameters.}

\item{validation_split}{Fraction of data reserved for validation.}
}
\value{
An object of class \code{"GiVAE"}.
}
\description{
Trains an iVAE whose latent distribution is assumed to follow
a nonstationary spatial SAR model.
For each observation i the prior mean is
  \eqn{\mu_i = \rho(u_i) \sum_{j \in \partial i} w_{ij}\, \hat{z}_j}
where \eqn{\hat{z}_j = \text{encoder}(x_j, u_j)} and \eqn{\rho(u_i)} is a
location-specific coupling strength estimated from auxiliary data \eqn{u_i}.
The prior variance \eqn{\sigma^2(u_i)} is also nonstationary, providing the
identifiability leverage of standard iVAE. Edge weights \eqn{w_{ij}} are
either uniform (1/|N(i)|) or learned via a bilinear attention model on
\eqn{(u_i, u_j)}.
}
